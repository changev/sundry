diff --git a/vagrant/Vagrantfile b/vagrant/Vagrantfile
index 3b2fae1..d3dc908 100644
--- a/vagrant/Vagrantfile
+++ b/vagrant/Vagrantfile
@@ -63,12 +63,14 @@ Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|
         # Default value: false
         target.ssh.forward_agent = true
 
-        target.vm.provision "shell", inline: <<-SHELL
+        target.vm.provision "shell",
+          privileged: false, 
+	  inline: <<-SHELL
           service isc-dhcp-server start
           service rsyslog stop
           echo manual | sudo tee /etc/init/rsyslog.override
-          pm2 logs > /home/vagrant/src/"#{ENV['REPO_NAME']}"/vagrant.log &
-          pm2 start rackhd-pm2-config.yml
+          sudo pm2 logs > /home/vagrant/src/"#{ENV['REPO_NAME']}"/vagrant.log &
+          sudo pm2 start rackhd-pm2-config.yml
         SHELL
     end
 end
